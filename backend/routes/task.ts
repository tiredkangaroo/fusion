// import { Router } from "express";
// const taskRouter = Router();

// taskRouter.post("/newtask", async (req, res) => {
//   const { error } = await loginCheck(req);
//   if (error) {
//     res
//       .status(401)
//       .send("You are not logged in, therefore unpermitted to this operation.");
//   }
// });
